# 炼丹台隐私政策

更新日期：2026 年 9 月 16 日
生效日期：2026 年 9 月 16 日

炼丹台（包名：`com.modeltest.monitor`）是一款由 Zephyer 开发的训练监控工具。本政策说明炼丹台如何处理用户在使用 App 时涉及的数据。

## 1. 处理的数据

炼丹台不要求注册账号，不读取通讯录、定位、相册、麦克风、摄像头、短信或通话记录。

为提供训练监控功能，App 会在用户设备本地处理并保存：

- 用户主动填写的本地电脑面板地址；
- 用户主动填写的本地同步 Token；
- 刷新间隔、界面风格、通知开关和所选指标；
- 最近一次从用户指定的本地电脑面板同步的训练状态、指标和历史曲线缓存；
- 用户是否已阅读并同意隐私提示的本地状态。

本地同步 Token 使用 Android Keystore 支持的加密存储。App 已关闭 Android 系统备份，并排除文件、数据库和 SharedPreferences 的云端迁移。

## 2. 使用目的

上述数据仅用于：

- 连接用户指定的本地电脑只读同步接口；电脑端负责通过 SSH 读取训练服务器；
- 展示 Epoch、训练进度、指标、最佳结果、预计剩余时间和历史曲线；
- 在用户主动开启后显示训练状态和训练完成通知；
- 在网络中断时显示最后一次成功同步的本地缓存。

炼丹台不会将本地同步 Token 用于用户指定电脑以外的网络请求。局域网或 Tailscale/WireGuard 私有地址可由用户选择使用 HTTP；如果把接口暴露到公网，应使用 HTTPS。用户启用 Cloudflare Tunnel、ngrok 或其他公网隧道时，训练状态请求会经过用户选择的隧道服务中转；项目不会额外上传或保存这些数据。

## 3. 权限说明

- 网络权限：访问用户配置的本地电脑面板；
- 通知权限：在用户同意后显示训练状态和完成提醒；
- 前台服务权限：在通知功能开启时持续同步训练状态。

拒绝通知权限不会影响 App 内查看训练数据。

## 4. 第三方服务与共享

当前 Android App 不接入广告、统计、用户画像、支付或社交登录 SDK，不向开发者或第三方主动上传用户的训练状态与访问 Token，也不出售个人信息。Cloudflare Tunnel 等第三方隧道仅在用户主动启用电脑端公网转发时参与网络传输，具体处理受对应服务商政策约束。

用户配置的本地电脑和训练服务器由用户或其所在组织管理。电脑端如何通过 SSH 获取训练日志和指标，不属于炼丹台 Android App 的数据处理范围；用户应确认自己有权访问和处理相关设备与数据。

## 5. 保存期限与删除

本地配置和缓存会保存到用户主动清除、卸载 App 或撤回同意为止。用户可以在 App 的“设置 > 隐私与权限”中：

- 清除训练缓存；
- 清除本机 Token；
- 撤回同意并停止刷新与通知。

服务端 Token 可通过 `training-monitor rotate-token` 立即更换。

## 6. 未成年人保护

炼丹台面向进行模型训练和服务器运维的用户，不以未成年人为目标用户，也不主动收集年龄或身份信息。未成年人应在监护人指导下使用。

## 7. 政策更新

如数据处理方式、权限或第三方服务发生实质变化，App 将更新本政策，并在必要时重新征得用户同意。提交记录可在项目仓库中查询。

## 8. 联系方式

- 项目主页：https://github.com/ZephYer8/training-monitor
- 问题反馈：https://github.com/ZephYer8/training-monitor/issues

如需查询、更正或删除与炼丹台有关的本地数据，可通过上述渠道联系，或直接使用 App 内的清除与撤回功能。
